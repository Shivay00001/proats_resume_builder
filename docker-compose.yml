version: '3.8'

services:
  builder:
    build: .
    environment:
      - DISPLAY=${DISPLAY}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
      - ./:/app
    # Note: Running GUI apps in Docker requires X11 forwarding configured on the host.
